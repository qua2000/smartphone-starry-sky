# 確認メモ

## ① カメラ映像（Android Chrome）

- ユーザーが実機で確認済み。
- 結果：`映った`。

## ② 現在地・現在時刻（実装済み、確認待ち）

- 公開確認URL上で、画面の見出しが「スマホ星空：② 位置と時刻の確認」と表示されることを確認した。
- 初期状態に「カメラを開始する」と「現在地を取得する」の操作がある。
- カメラを開始した後、時刻を1秒ごとに表示し、現在地の許可後に緯度・経度・精度を表示する設計である。
- スマホ実機での位置情報取得は、次のユーザー確認待ちである。

## ③ 方位角・傾き（実装済み、確認待ち）

画面に「スマホの向きを取得する」ボタンを追加した。ボタンを押すと、方位角の目安、前後の傾き、左右の傾き、センサー状態が更新される。スマホをゆっくり回したり傾けたりして、数値が変化することをユーザーに確認してもらう。

## ④ 月の方位角・高度（実装済み、確認待ち）

現在地を取得した後に「月の位置を計算する」ボタンを押すと、SunCalc 2.0.1を用いて、その時刻・その緯度経度から見た月の方位角と高度を表示する。高度が0°より大きければ、計算上は月が地平線より上にある。数値が表示されることと、地平線より上か下かの説明が表示されることをユーザーに確認してもらう。

### ④の事前確認メモ

初期指定したSunCalc 2.0.1のCDN URLでは、ブラウザ上のグローバル変数 `SunCalc` を取得できなかった。スマホ実機で試す前に、ライブラリの正しいブラウザ用配布ファイルへ変更して再確認する。

修正後は、公式ドキュメントで示されたESモジュール用のCDN URLを使う方式に変更した。ページ読み込み時にブラウザのエラー出力がないことを確認した。

## ⑤ 月の予測位置の重ね表示（実装済み、確認待ち）

月の方位角・高度から地球基準の3次元ベクトルを作り、端末のalpha・beta・gammaでスマホ基準へ変換した上で、カメラ画面の座標へ投影する処理を追加した。月の高度が0°以下であれば丸印を隠し、月が地平線より上でも視野外なら丸印を隠して案内文を出す。月が地平線より上にあり、スマホを月の方位角・高度に近い向きへ向けたとき、カメラ映像上に琥珀色の丸印が表示されることを確認してもらう。

⑤の画面には、月の予測位置を重ねるボタンが表示され、読み込み時のブラウザJavaScriptエラーがないことを確認した。

## ⑥ NameボタンとMoon表示（実装済み、確認待ち）

月の予測位置を表す琥珀色の丸印が画面内にある場合だけ、Nameボタンを有効にする処理を追加した。Nameを押すと、丸印の近くにMoonと表示され、もう一度押すと名前を隠す。Nameを押してMoonが表示されることをユーザーに確認してもらう。

⑥の画面にはNameボタンが表示され、読み込み時のJavaScriptエラーがないことを確認した。途中で一時確認用の公開ページが停止したため、最新ファイルを配信するサーバーを再起動して復旧した。
