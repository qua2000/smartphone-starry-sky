# 主要な一等星と夏の大三角：AR拡張サンプル

`major-stars-ar-sample.html` は、ベガ1個のARサンプルを、複数の主要な一等星と「夏の大三角」へ拡張した独立した学習用ページです。既存の月ARページとベガ1個のページは変更しません。

このサンプルでは、**シリウス、ベガ、アルタイル、デネブ、アークトゥルス**を同じ手順で計算します。各星の赤経・赤緯を、現在地と現在時刻を基に方位角・高度へ変換し、カメラの視野内にある星だけを予測位置の点として描きます。ベガ・アルタイル・デネブが同時に画面内に入った場合は、透明なCanvasに3本の線を描き、夏の大三角を表します。

## 試すURLと操作順

GitHub Pagesを公開済みの場合、このページのURLは次のとおりです。

```text
https://qua2000.github.io/smartphone-starry-sky/major-stars-ar-sample.html
```

一時確認用URLを使う場合は、ベースURLの末尾を `/major-stars-ar-sample.html` にしてください。

| 順番 | 操作 | 画面で確認すること |
|---|---|---|
| 1 | `カメラを開始する` を押す | 後ろ向きカメラが表示される |
| 2 | `現在地を取得する` を押す | 緯度・経度が表示される |
| 3 | `スマホの向きを取得する` を押す | 方位の数字が変化する |
| 4 | `主要な一等星を表示する` を押す | 画面内にある星だけが白や淡青、淡橙の点で表示される |
| 5 | `夏の大三角の線を表示する` を押す | ベガ・アルタイル・デネブの3点が同時に画面内なら、3本の点線が結ぶ |

> **最初の成功条件**は、まず一つでも星の点が現れ、「画面内の星」に星名が表示されることです。次に、スマホをゆっくり動かすと星の点が画面内を移動することを観察してください。夏の大三角の3星は広い範囲に並ぶため、横向き（ランドスケープ）で空を広く映すと、3点が同時に画面へ入りやすくなります。

## 1個版から何を変えたか

ベガ1個版では、定数 `VEGA` と `updateVegaOverlay()` が中心でした。複数星版では、星を1個ずつ書く代わりに、星データを配列 `BRIGHT_STARS` に入れ、全ての星に共通の関数 `calculateStarHorizontalPosition()` を繰り返し適用します。この変更が、星を増やす基本です。

| ベガ1個版 | 複数星版 | 役割 |
|---|---|---|
| `const VEGA = {...}` | `const BRIGHT_STARS = [...]` | 星を1個ではなく配列で管理する |
| `calculateVegaHorizontalPosition()` | `calculateStarHorizontalPosition(star, ...)` | どの星にも使える位置計算関数にする |
| `vegaMarker` | `starMarkers`（Map） | 表示中の星ごとに点を作り、管理する |
| なし | `constellationCanvas` | 星と星を結ぶ線だけを描く透明な描画面 |
| なし | `SUMMER_TRIANGLE` | どの3星をどの順に結ぶかを定義する |

## 星データを追加する方法

星を増やすときは、`BRIGHT_STARS` 配列へ1件ずつ追加します。必要なのは、内部用のID、日本語・英語名、J2000基準の赤経・赤緯、等級、見た目の色と大きさです。現在のサンプルは、SIMBADに掲載されたICRS/J2000座標とV等級を使用しています。[1]

```js
{
  id: "star-id",                 // 英小文字の一意なID
  nameJa: "日本語の星名",
  nameEn: "English name",
  raHoursJ2000: 0.0,              // 赤経：時単位の小数
  decDegJ2000: 0.0,               // 赤緯：度単位の小数
  magnitude: 1.0,                 // 見かけの等級
  color: "#d9edff",              // 点の色
  markerSize: 12                  // 点の大きさ（px）
}
```

| サンプルの星 | 赤経（時） | 赤緯（度） | V等級 | 用途 |
|---|---:|---:|---:|---|
| シリウス | 6.752477022222 | -16.716115861111 | -1.46 | 冬の明るい星としての確認用 |
| ベガ | 18.615648986111 | 38.783688944444 | 0.03 | 夏の大三角の頂点 |
| アルタイル | 19.846388486110 | 8.868321194443 | 0.76 | 夏の大三角の頂点 |
| デネブ | 20.690531983333 | 45.280338805554 | 1.25 | 夏の大三角の頂点 |
| アークトゥルス | 14.261020019444 | 19.182409166666 | -0.05 | 春の明るい星としての確認用 |

## 夏の大三角の線を描く仕組み

星座や星の並びを線で描くには、先に各星の画面上の位置を計算します。そのうえで、結びたい星のIDの組を `edges` に書きます。このサンプルでは、夏の大三角を星座とは区別し、三つの星を結ぶアステリズムとして扱っています。

```js
const SUMMER_TRIANGLE = {
  nameJa: "夏の大三角",
  starIds: ["vega", "altair", "deneb"],
  edges: [
    ["vega", "altair"],
    ["altair", "deneb"],
    ["deneb", "vega"]
  ]
};
```

`drawSummerTriangle()` は、`edges` の各組について、始点・終点の星の画面X・Y座標を取り出してCanvasの `moveTo()` と `lineTo()` で結びます。三つの星のうち一つでも画面外なら、線は描きません。画面外を横切る不自然な線を避けるためです。

| 処理の順番 | 関数 | していること |
|---|---|---|
| 1 | `calculateStarHorizontalPosition()` | 星表座標から方位角・高度を求める |
| 2 | `projectAzimuthAltitudeToScreen()` | 方位角・高度から画面X・Yを求める |
| 3 | `updateStarMarkers()` | 画面内にある星の点を作る・動かす・消す |
| 4 | `drawSummerTriangle()` | 3星が画面内のときだけ線を引く |

IAUが定める星座は空の領域であり、星と星を結ぶ図柄は公式に一意には定められていません。そのため、アプリで描く線は「どの星を結ぶか」という表示ルールとして自分で管理します。[2]

## 表示されないときの見方

このサンプルは、画面を点で埋めないよう、また地平線近くの不安定な表示を避けるように、**高度10°以下の星を隠す**設定です。これは星が消えたという意味ではなく、現在の設定で予測マーカーを出さないという意味です。

| 状況 | 意味 | 試すこと |
|---|---|---|
| `画面内の星` が0個 | 対象星が地平線の下、低空、またはカメラの視野外 | 夜空の別の方向をゆっくり向く |
| 点は出るが夏の大三角の線が出ない | 3星のうち少なくとも1星が画面外 | スマホを横向きにして空を広く入れる |
| 点が小刻みに振動する | 方位センサーの微小な揺れ | スマホを静かに持ち、次の校正機能で改善する |
| 実際の星と点が少しずれる | カメラ画角・方位センサーの個体差 | 月や明るい星に合わせる校正を追加する |

## 次に星座を増やす方法

夏の大三角が確認できたら、同じデータ構造でオリオン座などを追加できます。最初は、星座を構成する星だけを `BRIGHT_STARS` へ追加し、線を一本ずつ `edges` に足してください。何十もの星・線を一度に追加するより、**2星を結ぶ1本の線**を確認してから増やす方が、座標と描画の問題を見つけやすくなります。

## 参考資料

[1]: https://simbad.cds.unistra.fr/simbad/sim-id?Ident=Vega&output.format=ASCII "SIMBAD: Vega（ほかの星も同じSIMBAD検索で確認）"
[2]: https://iauarchive.eso.org/public/themes/constellations/ "International Astronomical Union: The Constellations"
[3]: https://www.npmjs.com/package/astronomy-engine "Astronomy Engine JavaScript/TypeScript documentation"
