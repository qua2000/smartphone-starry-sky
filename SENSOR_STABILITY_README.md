# 方位・位置のぶれを抑える安定化版

`sky-ar-stable.html` は、統合版 `sky-ar-complete.html` に対し、**方位角の異常な細かいぶれ**と、位置情報の不安定さを確認・緩和する処理を加えたページです。センサーが完全に正確になるわけではありませんが、値をそのまま使うのではなく、品質を表示してから、急な飛びを除外し、時間に応じてなめらかにします。

> GPSは主に「現在地」を決めるためのもので、方位角の激しいぶれの主因は通常、電子コンパス（磁気センサー）や端末姿勢センサーです。位置の精度を上げても、方位センサーの揺れそのものは解決しません。

## 追加した改善

| 改善 | 何をするか | 期待できる効果 |
|---|---|---|
| 絶対方位の優先 | `deviceorientationabsolute` を先に利用する | 地球基準の方位データを利用できる端末では、相対方位の蓄積ずれを避けやすい |
| 二重イベントを避ける | 絶対方位が届く場合、通常の方位イベントを重ねて処理しない | 同じ姿勢を二度処理して不必要に揺れることを避ける |
| 外れ値除去 | 1秒あたり180°を超える急な跳びを捨てる | 磁気ノイズなどで数字が瞬間移動する影響を抑える |
| 時間ベース平滑化 | 時間定数0.75秒の指数移動平均を使う | 端末ごとにイベント頻度が違っても、ほぼ同じ滑らかさを保つ |
| 最良位置の採用 | 最大7秒間の高精度位置情報から、最小の `accuracy` を採用する | 一度だけ不正確な位置が返る影響を抑える |
| 方位補正 | スライダーで−30°〜+30°の補正を保存する | 実際の月や明るい星へ印を合わせるための最終調整ができる |
| 品質表示 | 方位の種類・外れ値数、位置の95%精度範囲を表示する | 「信じてよい状態か」を画面で判断できる |

## スマホで試す順番

安定化版は、次のURLで開けます。

```text
https://8080-igxd900hhtdau6obo07tc-5f886b00.sg1.manus.computer/sky-ar-stable.html
```

| 順番 | 操作 | 見る場所 |
|---:|---|---|
| 1 | `カメラを開始する` を押す | カメラ映像と情報カードが出る |
| 2 | `現在地を取得する` を押す | 「最大7秒間…」の後、**位置の品質**に最良精度が出る |
| 3 | `スマホの向きを取得する` を押す | スマホを動かした後、2秒ほど静かに持ち、**方位の品質**を読む |
| 4 | 月または明るい恒星を表示する | 印が以前よりゆっくり動くか確認する |
| 5 | 実物が確認できるときだけ方位補正を少し動かす | 印を実物に合わせ、補正値を記録する |

`方位の品質` が **絶対方位** と表示されれば、端末が絶対方位データを返しています。`相対方位` と表示される場合は、ページ開始後の向きを基準にする情報なので、ARの方角合わせは限定的になります。Device Orientation APIでは `absolute` が、絶対的な方位情報が得られているかを表します。[1]

位置の品質に表示する「約○m」は、緯度・経度の精度を95%信頼水準でメートル表示した値です。数値が小さいほど、現在地として採用しやすい状態です。[2]

## 方位補正を使う際の注意

方位補正は、センサー自体を校正する機能ではありません。**端末・持ち方・カメラ視野角による一定のずれを、表示位置へ加えるための補正**です。月、金星、木星、ベガなど、肉眼で確認できる目印がある夜にだけ、0.5°ずつ小さく動かしてください。補正値はブラウザ内に保存され、`補正をリセット` を押すと0°へ戻ります。

金属製の机、スピーカー、マグネット付きケース、車内などでは、電子コンパスが大きく乱れることがあります。その場合は、場所を少し移動し、端末ケースを外して、スマホ本体のコンパス校正を行ってから試してください。アプリの数式だけで磁気的な干渉を取り除くことはできません。

## 位置情報について

`enableHighAccuracy: true` は、ブラウザへより高い精度の位置取得を希望する指定です。ただし、取得が遅くなったり、バッテリー消費が増えたりする可能性があります。[3] この安定化版では常時追跡は行わず、位置更新ボタンを押したときだけ、最大7秒間の候補から一番よい値を選びます。

## ブラウザでの検証記録

安定化版はブラウザで読み込み、方位イベントを模した値を2回与えて検証しました。表示された方位は北付近で安定し、品質欄には `絶対方位／外れ値除去 0回／平滑化 0.75秒` と出ました。その後、補正を+5.0°に変えると、表示用の方位が約5°変わることを確認しました。実際のセンサー精度とAR位置の最終確認は、スマホ実機で行ってください。

## 参考資料

[1]: https://developer.mozilla.org/en-US/docs/Web/API/DeviceOrientationEvent "MDN: DeviceOrientationEvent"
[2]: https://developer.mozilla.org/en-US/docs/Web/API/GeolocationCoordinates/accuracy "MDN: GeolocationCoordinates.accuracy"
[3]: https://developer.mozilla.org/en-US/docs/Web/API/Geolocation/getCurrentPosition "MDN: Geolocation.getCurrentPosition()"


## トップページ反映後の再確認

安定化版を通常のトップページにも反映した後、擬似的な絶対方位イベントを用いて再確認しました。北付近の値は `360.0°`、品質欄は `絶対方位／外れ値除去 0回／平滑化 0.75秒` と表示されました。方位補正を−3.5°へ変更すると、表示用の方位が `356.5°` へ変化することも確認しました。
