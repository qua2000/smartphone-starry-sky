# ベガ1個のAR表示サンプル

このフォルダーの `vega-ar-sample.html` は、既存の `index.html` を変更せずに、恒星のベガ1個をカメラ映像へ重ねて表示する学習用サンプルです。

## 何をしているサンプルか

このページは、ベガの赤経・赤緯という星の住所を使い、現在地と現在時刻から、ベガが今どの方角・どの高さにあるかを計算します。その結果を、スマホの方位・傾きと比べ、カメラ映像の中にある場合だけ白青色の印として描きます。

| 使う情報 | 役割 |
|---|---|
| ベガの赤経・赤緯 | 星表に登録された、天球上のベガの住所 |
| 現在地 | その場所から見たベガの位置を求めるために使う |
| 現在時刻 | 地球の自転により、ベガが空のどこに見えるかを求めるために使う |
| スマホの方位・傾き | カメラがベガの方向を向いているかを判断するために使う |

> 白青色の印は、カメラがベガを画像認識した結果ではありません。計算したベガの予測位置です。

## ベガのデータ

サンプルでは、SIMBADに掲載されているVega（α Lyrae、HD 172167）のICRS/J2000座標を小数値へ変換して使用しています。赤経は18時36分56.33635秒、赤緯は+38度47分01.2802秒で、V等級は0.03です。[1]

| 項目 | サンプル内の値 | 意味 |
|---|---:|---|
| `raHoursJ2000` | 18.615648986 | 赤経。時単位の小数 |
| `decDegJ2000` | 38.783688944 | 赤緯。度単位の小数 |
| `magnitude` | 0.03 | 見かけの明るさの目安 |

## 使い方

GitHub Pagesを公開済みの場合は、次のURLで開けます。

```text
https://qua2000.github.io/smartphone-starry-sky/vega-ar-sample.html
```

まだGitHub Pagesを公開していない場合は、先に `GitHub_Pages公開手順.md` の手順で公開設定を行います。GitHub Pagesでの公開URLが利用できるまでは、一時確認用URLの末尾に `/vega-ar-sample.html` を付けても試せます。

スマホでは、次の順で操作してください。

| 順番 | 操作 | 確認する結果 |
|---|---|---|
| 1 | `カメラを開始する` を押し、使用を許可する | 後ろ向きカメラ映像が表示される |
| 2 | `現在地を取得する` を押し、使用を許可する | 緯度・経度が表示される |
| 3 | `スマホの向きを取得する` を押す | 方位の数字が表示される |
| 4 | `ベガの予測位置を表示する` を押す | ベガの方位角・高度・状態が表示される |
| 5 | ベガの方位角・高度を目安にスマホを動かす | ベガが画面内なら、白青色の印が出る |

## 白青色の印が出ない場合

ベガは季節や時刻によって地平線の下にあります。また、このサンプルでは高度が10°以下のときは、建物・樹木・大気の影響を避けるために意図的に印を隠します。ベガが地平線より上でも、カメラが別の方向を向いていれば「カメラの画面外」と表示されます。

| 表示の状態 | 意味 | 次にすること |
|---|---|---|
| 地平線より下 | 現在は計算上、空にない | 時間を変えて試す |
| 低空のため非表示 | 高度が0°より大きく10°以下 | 高度が高くなる時間を待つ |
| 地平線より上、カメラの画面外 | ベガは空にあるが、画面に入っていない | 方位角・高度へ近づくようにスマホを動かす |
| カメラ映像内に予測位置を表示中 | ベガの印を表示できた | 実際の星とのずれを観察する |

## コードを読む順番

`vega-ar-sample.html` は、コード内に番号付きのコメントを付けています。順番に読むと、月ARと恒星ARの違いを把握できます。

| コメント番号 | 読む内容 |
|---|---|
| 1 | ベガの赤経・赤緯・明るさのデータ |
| 2 | HTMLのボタンや表示欄をJavaScriptから操作する準備 |
| 3 | 現在地や端末姿勢を一時的に覚えておく変数 |
| 4 | カメラ、位置情報、端末姿勢を取得する処理 |
| 5 | ベガの赤経・赤緯を方位角・高度へ変換する処理 |
| 6 | 方位角・高度をカメラ画面のX・Y座標へ変換する処理 |
| 7 | ベガが画面内なら、白青色の印を表示する処理 |

## 正確さについて

天文学的な方位角・高度の計算にはAstronomy Engine 2.1.19を使っています。同ライブラリの `Horizon` 関数は、日時・観測地・赤経・赤緯から観測者にとっての方位角・高度を計算します。[2] 一方、画面上の印の位置は、スマホごとのカメラ画角と方位センサーの誤差の影響を受けます。

このため、白青色の印が実際のベガへ完全に一致しなくても、最初の確認としては問題ありません。次に実装するとよい機能は、実際の月や明るい星を使い、印を手動で合わせてずれを保存する**校正機能**です。

## 参考資料

[1]: https://simbad.cds.unistra.fr/simbad/sim-id?Ident=Vega&output.format=ASCII "SIMBAD: Vega"
[2]: https://www.npmjs.com/package/astronomy-engine "Astronomy Engine JavaScript/TypeScript documentation"
