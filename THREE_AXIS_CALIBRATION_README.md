# 月・金星を目印にする3軸校正と視野角調整

この更新では、これまでの方位（左右）だけの補正を、**方位・上下・左右傾きの3軸**へ広げ、さらに端末ごとのカメラの見え方に合わせる**縦の視野角**を調整できるようにしました。校正値は、同じブラウザの保存領域に記録され、次回開いたときにも使われます。

> 校正は天体の計算値を変えるものではありません。**計算で求めた天体を、カメラ映像のどこに置くか**を、そのスマホのセンサーとカメラに合わせて調整するものです。

## 追加した4つの調整

| 調整項目 | 画面上で動く方向 | 使う場面 | 初期値 |
|---|---|---|---:|
| 方位（左右） | 印が左右方向にずれる問題 | 月・金星の方角へ向けても印が横にずれる | 0.0° |
| 上下のずれ | 印が上・下にずれる問題 | 目印を画面中央に置いたのに印が上または下へ外れる | 0.0° |
| 左右傾き | 星座線や複数の印が斜めに傾く問題 | スマホをまっすぐ持っても印の並びが回転して見える | 0.0° |
| 縦の視野角 | 中心から離れるほど大きくなるずれ | 中央では合うが、画面の端で印が内側・外側へずれる | 43° |

視野角は、値を大きくすると「広角」、小さくすると「狭角」として扱います。画面中央の印はほぼ動かず、端に近い印ほど位置が変わります。この性質を利用して調整します。

## スマホでの校正手順

トップページをAndroid Chromeで開きます。

```text
https://8080-igxd900hhtdau6obo07tc-5f886b00.sg1.manus.computer/
```

最初に、通常どおり`カメラを開始する`、`現在地を取得する`、`スマホの向きを取得する`を順に行います。スマホを数秒ゆっくり動かした後、静かに持って、方位の品質が落ち着いてから始めてください。

次に、**夜間に肉眼で安全に確認できる月または金星**を選びます。太陽に近い方向の金星を探すために空へスマホを向けることはしないでください。月が見えている夜なら、月を目印にする方法が最初は分かりやすく安全です。

| 順番 | 操作 | 調整の考え方 |
|---:|---|---|
| 1 | `3軸校正を開く` を押す | 校正パネルを開く |
| 2 | `月を目印にする` を選び、月をカメラ画面の中央近くへ入れる | 実物の月とアプリ上の`Moon`印を見比べる |
| 3 | `目印を表示する` を押す | 月の印と名前を自動で表示する |
| 4 | **方位（左右）**を0.5°ずつ動かす | 印を実物の月と同じ左右位置に近づける |
| 5 | **上下のずれ**を0.5°ずつ動かす | 印を実物の月と同じ上下位置に近づける |
| 6 | スマホを縦にまっすぐ持ち、**左右傾き**を少しずつ動かす | 近くの星の並びや印の傾きが自然になるよう調整する |
| 7 | 月を画面の端へゆっくり移動し、**縦の視野角**を1°ずつ動かす | 端でも印が実物から離れすぎない値を探す |

一度に複数のつまみを動かさないことが重要です。1項目を少し動かして、実物と印が近づくかを確認してから次へ進みます。思わしくない場合は`校正をリセット`を押すと、方位・上下・左右傾き・視野角を初期値に戻せます。

## 金星を使う場合

金星が日没後または日の出前に十分高く、肉眼で見えているときだけ使います。`金星を目印にする`を選んで`目印を表示する`を押すと、惑星表示と名前表示がオンになります。画面内に金星があれば、`金星`という印を目印にして同じ順で調整できます。アプリは太陽から15°未満、高度10°以下の惑星を安全のため表示しません。

## 実装上の仕組み

各スライダーの値は`localStorage`の`sky-ar-calibration-v3`へ保存されます。元の方位補正の値が存在する場合は、その値を最初の方位補正として引き継ぎます。投影関数では、方位補正を方位角へ、上下補正を端末の前後回転へ、左右傾き補正を端末の左右回転へ、視野角を透視投影の倍率へ反映します。

座標計算には月・惑星・恒星の天文位置を求めるAstronomy Engineを使用しています。[1] 端末の向きはDevice Orientation API、現在地はGeolocation APIから得ます。Device Orientation APIの`absolute`は、絶対的な座標系に対する端末の向きが得られているかを示します。[2] 位置情報の`accuracy`は、緯度・経度について95%信頼水準で表したメートル単位の精度範囲です。[3]

## ブラウザでの検証

トップページに`3軸校正を開く`と`校正をリセット`が表示されることを確認しました。仮の観測地・端末姿勢で、方位`+3°`、上下`−2°`、左右傾き`+1°`、縦の視野角`50°`へ順に変更したところ、校正状態へ正しく反映され、リセット操作で`0° / 0° / 0° / 43°`へ戻ることを確認しました。実物の月・金星との最終調整は、必ずスマホ実機で行ってください。

## 参考資料

[1]: https://github.com/cosinekitty/astronomy "Astronomy Engine"
[2]: https://developer.mozilla.org/en-US/docs/Web/API/DeviceOrientationEvent "MDN: DeviceOrientationEvent"
[3]: https://developer.mozilla.org/en-US/docs/Web/API/GeolocationCoordinates/accuracy "MDN: GeolocationCoordinates.accuracy"
